# Contributing

## Development Setup

```bash
make dev-setup
```

## Before Opening A Pull Request

```bash
make pre-commit
```

## Guidelines

- Keep changes focused and covered by tests when behavior changes.
- Run code generation after editing Freezed, JSON, Retrofit, or Injectable files.
- Use the pinned Flutter SDK through FVM.
